Model is static.
Controller determines what is moved / displayed


const app = new Controller(new Deck(), new View());


so controller is the central thing


things happen because functions/or some shit in the controler are bound.


Model
        bindDeckChanged(callback) {
            this.onDeckChanged = callback;
        }




Controller

    this.model.bindDeckChanged(this.onDeckChanged);


        onDeckChanged = cards => {
            this.view.displayDeck(cards);
        }



View
        displayDeck(cards) {
            while (this.deckList.firstChild) {
                this.deckList.removeChild(this.deckList.firstChild);
            }

        etc